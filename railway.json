{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install --no-audit --no-fund && npm run build && npm install --no-audit --no-fund --prefix server && npm run db:generate --prefix server"
  },
  "deploy": {
    "startCommand": "npm run db:push --prefix server && npm run db:seed --prefix server && npm start --prefix server"
  }
}

