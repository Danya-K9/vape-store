{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install --no-audit --no-fund && npm run build && npm install --no-audit --no-fund --prefix server && npm run db:generate --prefix server && npm run db:push --prefix server && npm run db:seed --prefix server"
  },
  "deploy": {
    "startCommand": "if [ -d server ]; then node server/index.js; else node index.js; fi"
  }
}

